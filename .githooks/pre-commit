#!/usr/bin/env bash
# Pre-commit hook: validates the compose file and all shell scripts
# (docker compose config -q, bash -n, shellcheck if installed).
# Activate once with scripts/setup.sh (sets core.hooksPath = .githooks).
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
exec scripts/check.sh
